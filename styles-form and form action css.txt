/* ---------- CONTACT FORM ---------- */
.contact-section {
  max-width: 600px;
  margin: 2rem auto;
  padding: 1rem;
}

.contact-section h1 {
  font-family: var(--font-heading);
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.contact-section p {
  margin-bottom: 1.5rem;
}

form label {
  display: block;
  margin-bottom: 0.3rem;
  font-weight: bold;
}

form input,
form textarea {
  width: 100%;
  padding: 0.75rem;
  margin-bottom: 1rem;
  border: 1px solid var(--gray);
  border-radius: 4px;
  font-family: var(--font-body);
}

form button {
  background-color: var(--primary);
  color: white;
  border: none;
  padding: 0.75rem 1.5rem;
  cursor: pointer;
  font-size: 1rem;
  border-radius: 4px;
  transition: background-color 0.3s ease;
}

form button:hover {
  background-color: #870b0a; /* slightly darker */
}


media queries ( final the archive)   
 .contact-section {
        max-width: 800px;
        padding: 2rem;
    }
        
    form {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
    }
        
    form label {
        grid-column: span 2;
    }
        
    form input,
    form textarea {
        grid-column: span 2;
    }
        
    form button {
        grid-column: span 2;
        justify-self: start;
    }
}

